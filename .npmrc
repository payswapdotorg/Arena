# Arena frozen dependency policy (A001).
# - save-exact: every added dependency is pinned to an exact version.
# - engine-strict: the engines field (node >=22 <23) is enforced on install.
save-exact=true
engine-strict=true
